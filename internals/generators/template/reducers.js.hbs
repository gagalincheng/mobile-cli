/*
 *
 * {{ fileName }} reducers
 *
 */

import * as types from '../constants/actionTypes';
import { fromJS } from 'immutable';

const initialState = fromJS({

});

function {{ fileName }}Reducer(state = initialState, action) {

}

export default {{ fileName }}Reducer;
